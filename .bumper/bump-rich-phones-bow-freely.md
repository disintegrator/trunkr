---
trunkr: minor
---

Initial herdr plugin skeleton: the plugin links into herdr, a hello action verifies worktrunk (wt) is on PATH and calls back into herdr, and a Bubble Tea smoke-test pane proves the picker's TUI stack runs in an overlay pane.
